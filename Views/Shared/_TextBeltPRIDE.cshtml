@{
    string linkUrl = "https://www.ggoya.com/es";
    string linkText = "New in";

    if (ViewData["LinkUrl"] is string url) { linkUrl = url; }
    if (ViewData["LinkText"] is string txt) { linkText = txt; }
}



<!-- Contenedor -->
<div class="text-belt-wrapper">
    <div class="text-belt">
        <div class="text-track">
            @for (int i = 0; i < 16; i++)
            {
                <a href="@linkUrl" class="text-item">@linkText</a>
            }
        </div>
    </div>
</div>

<!-- Estilos -->
<style>
    .text-belt-wrapper {
        width: 100%;
        overflow: hidden;
        background: linear-gradient(135deg, #e0c3fc 0%, #8ec5fc 100%);
        padding: 30px 0;
        position: relative;
    }

    .text-belt {
        overflow: hidden;
        position: relative;
    }

    .text-track {
        display: flex;
        width: fit-content;
        animation: scroll-text-belt 25s linear infinite;
        gap: 100px;
    }

    .text-item {
        font-family: 'Cormorant', serif;
        font-style: italic;
        font-weight: 500;
        font-size: 48px;
        color: #ffffff;
        white-space: nowrap;
        text-transform: lowercase;
        letter-spacing: 2px;
        text-decoration: none;
        transition: transform 0.3s ease, opacity 0.3s ease;
        opacity: 0.9;
        cursor: pointer;
    }

    .text-item:hover {
        transform: scale(1.05);
        opacity: 1;
    }

    @@keyframes scroll-text-belt {
        0% {
            transform: translateX(0);
        }
        100% {
            transform: translateX(-50%);
        }
    }

    .text-belt:hover .text-track {
        animation-play-state: paused;
    }

    @@media (max-width: 768px) {
        .text-track {
            gap: 60px;
        }

        .text-item {
            font-size: 32px;
        }
    }

    @@media (max-width: 480px) {
        .text-track {
            gap: 40px;
        }

        .text-item {
            font-size: 24px;
            letter-spacing: 1px;
        }
    }
</style>